{"name":"replace-site","on":{"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"replace":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"run":"cp aurenta-upload.zip /tmp/site.zip && find . -mindepth 1 -maxdepth 1 ! -name .git ! -name .github -exec rm -rf {} + && unzip -o /tmp/site.zip -d . && git config user.name github-actions && git config user.email github-actions@github.com && git add -A && git commit -m Aurenta && git push"}]}}}
